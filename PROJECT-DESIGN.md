# Ink & Memory Landing Page Design

## 页面体验目标

把 Ink & Memory 从普通产品介绍页升级为 React/Vite 架构内的 12 屏沉浸式转化页面。用户滚动时逐步理解：这是给认真写作者和自我探索者的工具；AI 可以参与写作，但关键写操作必须确认；长期写作会沉淀为记忆、模式、图像和自我档案。

## 信息架构

1. 品牌首屏：建立“写下来，让 AI 和你一起听见自己”的核心认知。
2. ICP 与痛点：让长期写作者、自我探索者、情绪记录用户快速判断“这是给我的”。
3. AI 真能改稿：突出最大差异化，同时说明确认机制。
4. 四种 AI 工具：明确写入、删除、插入、评论的边界。
5. 自动/逐步模式：展示可控性与灵活性。
6. 会呼吸的编辑器：呈现日常使用场景。
7. 带记忆的 AI 助手：突出长期上下文。
8. 声音角色团：表达情绪陪伴和多视角对话。
9. 深度回响分析：解释长期价值。
10. 每日 AI 图与好友时间线：展示社交但不暴露原文。
11. 安全与信任：建立转化前的边界信任。
12. 最终 CTA：收束到“开始今天的书写”。

## 动效策略

使用 CSS scroll-snap 做整屏吸附，React 内部的 IntersectionObserver 控制 active 状态。当前屏的标题、正文、视频和卡片分层淡入、轻微上移；离开时自然淡出。视频只在当前屏播放，其余屏暂停。动画节奏以 ease-out 和 soft cubic-bezier 为主，并支持 prefers-reduced-motion。

## 视频使用策略

5 个视频放在 `assets/video-1.mp4` 至 `assets/video-5.mp4`，由 `src/App.tsx` 通过 Vite `?url` 导入。为了保持透明贴纸感，页面实际主视觉优先循环使用全程黑底并已抠成透明 WebM 的 `video-3` 至 `video-5`；带统一场景背景的素材保留在 assets 中，后续需要场景化段落时可单独启用。

## 响应式策略

桌面端采用双列或三列沉浸布局，每屏保持 100vh。移动端改为文案、视频、内容卡片纵向排列，接近 100vh 但允许内容自然延展。移动端导航折叠，按钮保持足够点击面积，视频始终保留可见区域。
